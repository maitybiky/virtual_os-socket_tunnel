import { Typography } from "@mui/material";
import React from "react";

const Loading = ({ height, width, title = "" }: any) => {
  return <Typography>Loading {title} ...</Typography>;
};

export default Loading;
